schema_version: "1.1"
name: negative-context-budget
description: A context-management question is not delegable coding work.
tags: [negative]
runs: 3
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 10
  timeout_seconds: 300
  allowed_tools: [Read, Glob, Grep, Skill]
