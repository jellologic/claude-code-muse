schema_version: "1.1"
name: negative-single-file-edit
description: One trivial edit is cheaper done inline than delegated.
tags: [negative]
runs: 3
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 10
  timeout_seconds: 300
  allowed_tools: [Read, Glob, Grep, Skill, Edit, Write]
